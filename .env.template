# Local Development Environment Variables Template
# Copy this file to .env and fill in your local development values

# Database Configuration
POSTGRES_PASSWORD=your_postgres_password_here

# RabbitMQ Configuration  
RABBITMQ_PASSWORD=your_rabbitmq_password_here

# JWT Configuration (generate a secure key - minimum 256 bits)
JWT_SECRET_KEY=your_jwt_secret_key_here_minimum_256_bits

# Digital Ocean Spaces Configuration (for local development)
DO_SPACES_KEY=your_do_spaces_access_key
DO_SPACES_SECRET=your_do_spaces_secret_key

# Notes:
# - For local development, you can use simple passwords like 'nexora123'
# - Generate a strong JWT secret key (you can use online generators)
# - DO Spaces credentials are optional for basic local development
# - Never commit the actual .env file with real credentials to version control